# INSTALL
```$ pip install -r requirements.txt```

and this app Require an MS Edge to run

# FEATURES
get student detail from SIS-PSU and Transcript-PSU 
Using StudentID and Password to get info

## Example
`std = Student('64100XXXXXX', '1234567890')` will recieved an `std.info` then use PRINT's cell to print value in `std.info`